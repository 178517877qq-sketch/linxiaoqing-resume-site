---
title: 林晓庆个人简历网站项目管理说明
created: 2026-06-09
tags:
  - 简历网站
  - GitHub Pages
  - 数据分析作品集
  - 项目管理
---

# 林晓庆个人简历网站项目管理说明

## 1. 项目现状

这是一个用于对外展示的个人简历网站，定位不是普通简历页，也不是炫技型设计师作品集，而是“业务数据分析师 + 数据分析项目作品集”。

核心目标：

- 给招聘方、合作方或熟人快速看清专业定位。
- 展示业务数据分析、指标监控、数据治理、数据需求协同能力。
- 用真实项目作为能力证明，而不是只写抽象形容词。
- 保持红黑高级感，但内容优先，避免廉价大字和无意义装饰。

当前线上地址：

- GitHub 仓库：[https://github.com/178517877qq-sketch/linxiaoqing-resume-site](https://github.com/178517877qq-sketch/linxiaoqing-resume-site)
- 线上网站：[https://178517877qq-sketch.github.io/linxiaoqing-resume-site/](https://178517877qq-sketch.github.io/linxiaoqing-resume-site/)

本地项目路径：

```powershell
C:\Users\寻逆啊\Documents\优选订阅部署\cfnb-main -（调整优化使用）\linxiaoqing_resume_site
```

## 2. 这个项目有价值的东西

### 2.1 个人定位

当前网站定位为：

```text
林晓庆 | 业务数据分析师
业务数据分析 / 指标监控 / 数据治理 / 数据需求协同
```

这比“AI 视觉设计师”“高级作品集包装”更适合真实投递，因为它直接告诉别人你能解决什么业务问题。

### 2.2 三个项目证明

网站首页目前放了 3 个数据分析项目：

1. 经营数据分析与业务优化（广发银行）
   - 价值：展示数据派发规则、指标监控、对数复盘、接通率模型协同。
   - 关键表达：GMV +10%、外呼接通率 +8%、异常发现 T+3 提到 T+1。
   - 注意：涉及银行内部数据只写口径和结果，不展示明细。

2. 中国电商消费趋势分析
   - 价值：展示公开数据整理、官方口径、来源血缘、趋势报告。
   - 关键表达：线上零售规模、CAGR、实物线上化程度。
   - 注意：国家统计局、CNNIC、平台披露等来源要区分清楚。

3. 国际热门小说年度趋势分析
   - 价值：展示内容市场分析、样本清洗、题材趋势、热度代理指标。
   - 关键表达：Romance 集中度、年度峰值、Top10 热度占比。
   - 注意：`shelf_count` 是 Goodreads 平台热度代理，不等于销量。

### 2.3 专业表达方式

项目卡统一使用：

```text
业务问题 / 分析动作 / 输出价值
```

这个结构比单纯写“我做了什么”更专业，因为它能让别人看到：

- 你理解业务问题。
- 你知道如何拆解数据动作。
- 你能把结果转成可交付价值。

### 2.4 视觉资产

当前没有放头像，避免个人照影响专业感。

主要视觉资产在：

```text
public/assets
```

重点资产：

- `hero-data-flow-poster.jpg`
- `hero-data-flow.webm`
- `hero-data-flow.mp4`
- `project-stock-research.svg`
- `project-ecommerce-research.svg`
- `project-novel-market.svg`

这些资源已经做过体积控制，首屏视频和项目 SVG 都比较轻，不属于大体积资源。

## 3. 项目结构

核心文件：

```text
src/main.tsx      # 页面内容、项目数据、经历、优势、联系方式
src/styles.css    # 页面样式、响应式、动效、卡片效果
public/assets     # 图片、视频、SVG 项目封面
package.json      # 启动、构建、GitHub Pages 构建命令
```

重要命令：

```powershell
npm.cmd run dev
npm.cmd run build
npm.cmd run build:github
npm.cmd run preview
```

命令含义：

- `dev`：本地开发预览，地址通常是 `http://127.0.0.1:5173/`
- `build`：普通本地构建
- `build:github`：GitHub Pages 专用构建，带 `/linxiaoqing-resume-site/` 子路径
- `preview`：预览构建后的产物

## 4. Git 分支和部署方式

这个仓库现在有两条关键分支：

```text
main       # 源码分支，后续改代码主要改这里
gh-pages   # 线上发布分支，GitHub Pages 实际展示这里的 dist 产物
```

当前最终采用的是 `gh-pages` 分支部署方式。

注意：

- GitHub Actions 之前有历史红叉，原因是 Pages Actions 初始化/权限不稳定。
- 后来已经删除失败 workflow，避免后续继续红。
- 旧红叉是历史记录，不影响当前线上网站。
- 真正可用的线上页面来自 `gh-pages` 分支。

## 5. 后续修改网页的标准流程

### 5.1 进入项目目录

```powershell
cd "C:\Users\寻逆啊\Documents\优选订阅部署\cfnb-main -（调整优化使用）\linxiaoqing_resume_site"
```

### 5.2 修改源码

常见修改位置：

- 改文案、项目、经历、联系方式：`src/main.tsx`
- 改样式、排版、动效、移动端适配：`src/styles.css`
- 改图片、视频、项目封面：`public/assets`

### 5.3 本地预览

```powershell
npm.cmd run dev
```

打开：

```text
http://127.0.0.1:5173/
```

本地看没问题后再构建。

### 5.4 构建 GitHub Pages 版本

```powershell
npm.cmd run build:github
```

必须用 `build:github`，不要只用普通 `build` 来发布。

原因：

```text
GitHub Pages 项目页地址带 /linxiaoqing-resume-site/
```

如果不用 GitHub 专用构建，线上图片、视频、JS、CSS 容易路径错误。

### 5.5 提交源码到 main

```powershell
git add .
git commit -m "Update resume site"
git push
```

这一步只是保存源码，不等于更新线上页面。

### 5.6 发布到 gh-pages

构建后，把 `dist` 推到 `gh-pages` 分支。

推荐让 Codex 处理这一步，因为它会覆盖线上发布分支，手动操作容易把文件复制错。

如果必须手动执行，可以参考：

```powershell
cd "C:\Users\寻逆啊\Documents\优选订阅部署\cfnb-main -（调整优化使用）\linxiaoqing_resume_site"
npm.cmd run build:github

$deployDir = "C:\tmp\linxiaoqing-gh-pages"
if (Test-Path $deployDir) { Remove-Item -LiteralPath $deployDir -Recurse -Force }
New-Item -ItemType Directory -Path $deployDir -Force | Out-Null
Copy-Item -Path ".\dist\*" -Destination $deployDir -Recurse -Force
New-Item -ItemType File -Path "$deployDir\.nojekyll" -Force | Out-Null

git -C $deployDir init
git -C $deployDir branch -M gh-pages
git -C $deployDir config user.name "178517877qq-sketch"
git -C $deployDir config user.email "178517877qq-sketch@users.noreply.github.com"
git -C $deployDir add .
git -C $deployDir commit -m "Deploy resume site"
git -C $deployDir remote add origin "https://github.com/178517877qq-sketch/linxiaoqing-resume-site.git"
git -C $deployDir push --force origin gh-pages
```

发布后检查：

```text
https://178517877qq-sketch.github.io/linxiaoqing-resume-site/
```

## 6. 每次修改前的注意事项

### 6.1 不要碰外层项目

只改：

```text
linxiaoqing_resume_site
```

不要改 Cloudflare/IP 工具桌面界面相关文件。

外层目录里还有很多原项目文件，和简历网站无关。

### 6.2 不要把 dist 当源码维护

`dist` 是构建产物。

正确方式：

```text
改 src/public -> npm run build:github -> 推 gh-pages
```

不要直接长期修改 `dist`。

### 6.3 不要乱改资源路径

项目已经处理过 GitHub Pages 子路径问题。

代码里资源路径通过 `import.meta.env.BASE_URL` 适配：

```text
/linxiaoqing-resume-site/
```

如果新增图片或视频，优先放到：

```text
public/assets
```

再在代码里用项目已有的资源路径函数引用，不要直接写死 `/assets/...`。

### 6.4 数据指标不要随便写

这个网站重点是数据分析专业度，所以数字要能解释来源。

银行经营项目：

- 可以写口径治理、派发规则、指标监控、复盘机制和结果区间。
- 不要展示内部明细数据，提升幅度写"约 10%/约 8%"即可。

电商项目：

- 官方口径要区分“全国网上零售额”“实物商品网上零售额”“占社零比重”。
- 不要把 derived 指标写成官方直接披露。

小说项目：

- Goodreads 的 `shelf_count` 是平台热度代理。
- 不要写成销量。
- 不要把“每年 100 本”当成封面亮点，那只是采样规则，不是分析结论。

### 6.5 视觉不要跑偏

当前方向是：

```text
克制、专业、数据分析作品集
```

避免重新出现：

- AI 视觉设计师
- Prompt-ready portfolio
- 高级视觉表达
- 纯装饰大字
- 无意义指标卡
- 过度炫技动效

## 7. 常见优化方向

后续可以考虑：

1. 增加项目详情页
   - 银行经营分析详情
   - 电商趋势分析详情
   - 小说年度趋势详情

2. 增加 PDF 简历下载
   - 注意不要暴露地址。
   - PDF 里也不要放明文手机号，邮箱保留即可。

3. 增加项目图表交互
   - 先保证性能。
   - 不要引入大体积图表库，除非确实需要。

4. 优化 SEO
   - 增加 Open Graph 图。
   - 增加更完整的 description。

5. 优化移动端阅读
   - 每次改字体或卡片后都要检查手机宽度。
   - 避免字出框、卡片太挤、导航遮挡内容。

## 8. 部署验收清单

每次发布后检查：

```text
首页能打开：200
JS 能加载：200
CSS 能加载：200
首屏视频 poster 能加载：200
webm/mp4 能加载：200
三张项目 SVG 能加载：200
页面里能看到姓名、职位、三个项目、邮箱、手机号按钮（点击展示）
```

关键内容：

```text
林晓庆
业务数据分析师
经营数据分析与业务优化
中国电商消费趋势分析
国际热门小说年度趋势分析
xunni8214@gmail.com
手机号：点击展示（源码不含明文，防爬虫）
```

## 9. 一句话记忆

```text
main 管源码，gh-pages 管线上；改完先 build:github，再推 gh-pages。
```

