---
title: 林晓庆个人简历网站项目管理说明
created: 2026-06-09
updated: 2026-07-11
tags:
  - 简历网站
  - GitHub Pages
  - 数据分析作品集
  - 项目管理
---

# 林晓庆个人简历网站项目管理说明

> 本文档是该项目的唯一权威管理手册，面向任何后续维护者（人或 AI 助手，如 Claude / GPT / Codex）。
> 接手任何修改任务前，请完整阅读第 1、6、7 节；涉及数据改动必须先读第 5 节。

## 1. 项目定位与红线

这是一个对外展示的个人简历网站，定位是"业务数据分析师 + 数据分析项目作品集"，不是普通简历页，也不是炫技型设计师作品集。

核心目标：

- 给招聘方、合作方快速看清专业定位：业务数据分析 / 指标监控 / 数据治理 / 数据需求协同。
- 用真实项目和真实数据作为能力证明，而不是抽象形容词。
- 视觉保持红黑高级感，内容优先，克制专业。

**不可违反的红线（按重要性排序）：**

1. **数据真实性**：网站上每一个数字都必须可解释来源。封面图表、详情页表格、简历页数字全部有据可查（见第 5 节口径总表）。禁止为了排版好看编造或"美化"任何数字。
2. **敏感数据**：广发银行项目只写口径、机制和相对结果（约 10%、约 8%、T+3→T+1），禁止出现任何内部明细数据与绝对值。
3. **隐私**：手机号在源码中分段倒序存放（`PhoneReveal` 组件），禁止在任何文件中出现明文手机号（包括 resume.html）。邮箱 xunni8214@gmail.com 可以明文。
4. **视觉方向**：禁止回到"AI 视觉设计师""高级视觉表达"路线，禁止无意义大字、装饰性指标卡、过度动效。
5. **范围**：只改 `C:\project\linxiaoqing_resume_site` 文件夹内的文件，不触碰该文件夹以外的任何内容。

## 2. 地址与仓库

- GitHub 仓库：https://github.com/178517877qq-sketch/linxiaoqing-resume-site
- 线上网站：https://178517877qq-sketch.github.io/linxiaoqing-resume-site/
- SEO 限制：这是 GitHub Pages 项目站，`public/robots.txt` 发布后位于项目子路径，不能替代域名根目录的 `/robots.txt`；`sitemap.xml` 需通过 Google Search Console 等站长平台主动提交，或由用户名根 Pages 站点声明。
- 本地路径：`C:\project\linxiaoqing_resume_site`（2026-06-11 从"优选订阅部署\cfnb-main"下迁出，旧位置已废弃）
- 分支：`main` 管源码，`gh-pages` 管线上发布产物（dist）。GitHub Pages 从 gh-pages 分支提供服务。
- 技术栈：Vite 6 + React + TypeScript + Lucide 图标；无路由库、无 UI 组件库、无图表库。

## 3. 目录结构与文件职责

```text
linxiaoqing_resume_site/
├── index.html            # 首页 HTML：SEO meta、canonical、OG、JSON-LD Person
├── bank.html             # 银行项目独立 SEO 入口
├── ecommerce.html        # 电商项目独立 SEO 入口
├── novel.html            # 小说项目独立 SEO 入口
├── vite.config.ts        # React 插件 + 四入口构建配置
├── .github/workflows/ci.yml # main / PR 构建验证，不部署
├── src/
│   ├── main.tsx          # 全部页面内容与逻辑（单文件架构，刻意为之）
│   └── styles.css        # 全部样式（含详情页、响应式、动效、打印无关）
├── public/
│   ├── favicon.svg       # LXQ 红框图标
│   ├── resume.html       # 一页 A4 简历源页，?print=1 自动弹打印框
│   ├── robots.txt / sitemap.xml
│   └── assets/
│       ├── hero-data-flow-clean.jpg / og-share.jpg    # 无虚构数字首屏图 / 分享图
│       ├── lin-xiaoqing-data-analyst-resume.pdf       # 站内直接下载的一页简历
│       ├── ecommerce-*.csv / *.sql                    # 电商公开证据样例
│       ├── novel-methodology-sample.csv               # 小说清洗与口径样例
│       ├── project-bank-monitoring.svg                # 银行项目封面
│       ├── project-ecommerce-research.svg             # 电商项目封面
│       └── project-novel-market.svg                   # 小说项目封面
├── deploy.ps1            # 构建 + 推 gh-pages（单独发布）
├── push-source.ps1       # add/commit/push 源码到 main（单独存档）
├── publish-all.ps1       # 一键：推 main + 构建 + 发布 gh-pages（日常首选）
└── 项目管理说明.md        # 本文档
```

`src/main.tsx` 内部结构（改内容基本都在这些常量里，不需要动组件逻辑）：

| 常量/组件 | 内容 |
|---|---|
| `profile` | 姓名、职位、邮箱、教育、个人概述 |
| `stats` | 经历区四张统计卡（value + label + note 三段） |
| `projects` | 三张项目卡：id、标题、metric、业务问题/分析动作/输出价值、tags、封面图路径 |
| `projectDetails` | 三个项目详情页全文、项目元信息、来源/交付物与数据表格 |
| `strengths` | 核心能力四卡 |
| `timeline` | 五段工作经历（phase: primary / supporting / early） |
| `PhoneReveal` | 手机号点击展示组件（防爬虫） |
| `ProjectDetailPage` | 详情页渲染组件 |
| `App` | 根据独立 HTML 的 `data-project` 分发详情；旧 `?p=` 查询参数仅作兼容 |

## 4. 页面功能清单

1. **首页**：Hero（无数字数据流背景）→ 工具与交付条 → 精选项目（三张精简卡）→ 经历 → 核心能力 → 联系区。
2. **项目详情页**：`bank.html` / `ecommerce.html` / `novel.html`。每页有独立 title、description、canonical、OG、JSON-LD 和非 JS 摘要，正文为报告结构并提供来源/交付物。
3. **简历页**：`resume.html` 是一页 A4 源页；站内按钮直接下载 `assets/lin-xiaoqing-data-analyst-resume.pdf`。简历源页与 main.tsx 不共享数据，改经历/项目时**两处都要同步改并重新导出 PDF**。
4. **联系区**：手机号点击展示、邮箱、简历下载按钮。

## 5. 数据口径总表（改任何数字前必读）

### 5.1 银行项目（相对口径，敏感）

只允许出现：GMV 提升约 10%、外呼接通率提升约 8%、异常发现 T+3 → T+1、20+ 核心指标、2022.09-2025.12。封面图折线为**相对口径示意**（caption 已注明），详情页监控指标表只呈现体系结构不含真实值。

### 5.2 电商项目（全部已核实的公开数据）

网络购物用户规模与使用率（CNNIC《中国互联网络发展状况统计报告》，年末口径）：

| 年份 | 2021 | 2022 | 2023 | 2024 | 2025 |
|---|---|---|---|---|---|
| 用户规模（亿） | 8.42 | 8.45 | 9.15 | 9.74 | 9.37 |
| 占网民比例（%） | 81.6 | 79.2 | 83.8 | 87.9 | 83.2 |
| 来源 | 第 49 次 | 第 51 次 | 第 53 次（第 55 次回溯表交叉核验） | 第 55 次 | 第 57 次 |

网上零售额与实物线上化（国家统计局口径）：13.09 / 13.79 / 15.43 / 15.52 / 15.97 万亿元；官方可比口径同比 14.1 / 4.0 / 11.0 / 7.2 / 8.6 %；占社零比重 24.5 / 27.2 / 27.6 / 26.8 / 26.1 %。统计平台范围会变化，**禁止用相邻年度公布绝对额自行计算同比替代官方可比口径**。

核实渠道：CNNIC 官网统计报告列表 https://www.cnnic.net.cn/6/86/88/index.html 。更新数据时必须先查官网原文，禁止凭记忆写数。

### 5.3 小说项目（来源：本机「国际小说分析」项目）

- 数据源文件夹：`C:\Users\寻逆啊\Documents\国际小说分析`（年度索引在 `obsidian_vault\01-年度索引\`，图表在 `charts\`，趋势 CSV 在 `data\style_trends_by_year.csv`）
- 主风格五年变化（每年 Top 100 单标签口径）：Romance 25/29/33/30/28；Thriller 27/25/22/27/28；Romantasy 26/20/28/24/25；Historical 12/9/8/7/9；Literary 3/3/5/7/5
- 2025 分布：Romance 28、Thriller 28、Romantasy 25、Historical 9、Literary 5、Fantasy 2、Horror 2、SciFi 1
- 年度热度冠军（shelf_count）：2021 People We Meet on Vacation 394万；2022 The Housemaid 625万；2023 Fourth Wing 637万；2024 The Women 317万；2025 Onyx Storm 326万
- 2025 年 Top 100 样本 shelf_count 合计 61,588,277，即页面展示的 6158.8 万（四舍五入）
- 口径纪律：`shelf_count` 是 Goodreads 平台热度代理，**不等于销量**；"每年 100 本"是采样规则不是结论；主风格（单标签）与风格信号（多标签）两套口径不得混用

### 5.4 另一个未上站项目

`C:\Users\寻逆啊\Documents\优选订阅部署\cfnb-main -（调整优化使用）\小说研究资料总文件夹` 是**中文网文研究项目**（起点向，按年建档），目前未在网站展示，曾计划作为第四个项目卡，待用户决定。

## 6. 修改指南

### 6.1 常见修改的落点

| 想改什么 | 改哪里 |
|---|---|
| 文案、项目、经历、联系方式 | `src/main.tsx` 顶部各常量 |
| 项目详情页内容/表格 | `src/main.tsx` 的 `projectDetails` |
| 样式、排版、动效、移动端 | `src/styles.css` |
| 封面图表 | `public/assets/project-*.svg`（手写 SVG，直接编辑坐标） |
| 简历页 | `public/resume.html`（独立文件，注意与 main.tsx 同步） |
| SEO meta / 结构化数据 | `index.html` + 三个项目 HTML；新增页面同步更新 `vite.config.ts` 和 sitemap |

### 6.2 封面 SVG 的维护规则

- 三张封面 viewBox 均为 720×420，内容包在 `<g transform="translate(-43.2 -25.2) scale(1.12)">` 里（整体放大铺满）。
- 页面侧 `.project-visual` 用 `aspect-ratio: 12/7` + `object-fit: cover` 精确铺满，**改 SVG 尺寸比例时两边要一致**。
- **改图后必须同步更新 `src/main.tsx` 里该图的 `?v=日期-说明` 防缓存参数**，否则线上用户看到旧图。
- 图内数字必须与第 5 节口径总表一致。

### 6.3 资源路径规则

代码里资源一律通过 `assetPath()`（内部用 `import.meta.env.BASE_URL`）引用，适配 GitHub Pages 子路径 `/linxiaoqing-resume-site/`。新增图片放 `public/assets`，**禁止硬编码 `/assets/...` 绝对路径**。

### 6.4 内容风格约定

- 项目卡固定使用"业务问题 / 分析动作 / 输出价值"三段结构。
- 详情页固定报告结构：摘要 → 数据与方法 → 分章（可带表格）→ 结论与应用 → 边界说明。
- 拓展文案时只深化表达与专业细节，不新增无来源的数据指标。
- 三张项目卡的内容量尽量保持接近，避免高度参差留白。

## 7. 本地预览与发布流程

### 7.1 本地预览

```powershell
cd "C:\project\linxiaoqing_resume_site"
npm.cmd run dev
```

打开 `http://127.0.0.1:5173/`。详情页为 `/bank.html`、`/ecommerce.html`、`/novel.html`；简历源页为 `/resume.html`。

### 7.2 发布（日常用这个）

```powershell
& "C:\project\linxiaoqing_resume_site\publish-all.ps1"
```

publish-all.ps1 = `npm ci` + `build:github` 验证 + 白名单提交/推送 main + 发布 gh-pages。**构建成功后才允许推源码**。单步脚本：`push-source.ps1`（验证并推源码）、`deploy.ps1`（验证并发布）。三个脚本都会检查原生命令退出码。

### 7.3 构建命令说明

- `npm.cmd run build:github`：**发布必须用这个**，带 `--base=/linxiaoqing-resume-site/` 子路径。构建脚本是 `tsc && vite build`，TypeScript 报错会中断构建。
- `npm.cmd run build`：普通构建，只用于本地检查，**禁止用它的产物发布**（路径会全错）。
- `dist/` 是构建产物，禁止手改、禁止当源码维护。

### 7.4 回滚规则

- 大改必须在 `codex/` 功能分支进行，`main` 保持为最近稳定版本；发布前再合并或切回 main。
- 本轮优化前基线分支：`codex/pre-full-site-optimization-20260711`（提交 `fb28d0a`）。
- 发布脚本不接受非 main 分支，避免误把实验分支直接推上线。

### 7.5 已踩过的坑（务必避免）

1. **PowerShell 脚本编码**：Windows PowerShell 5.1 把无 BOM 的 UTF-8 .ps1 当 ANSI 解析，中文注释/字符串会乱码导致 ParseError。**所有 .ps1 脚本必须纯 ASCII**（现有三个脚本已是），路径用 `$PSScriptRoot` 自动定位，不要硬编码中文路径。
2. **GitHub Pages CDN 缓存约 10 分钟**：发布成功后线上短时间内仍是旧内容属正常。验证方法：URL 加随机查询参数（如 `?nocache=123`）绕过缓存，或等 10 分钟后 Ctrl+F5 强刷。不要因此重复发布。
3. **GitHub Actions 历史红叉**：仓库早期有失败的 Pages 部署 workflow，旧 workflow 已删除，红叉是历史遗留；当前仅保留 `.github/workflows/ci.yml`，负责验证 `main` 与面向 `main` 的 PR 构建，不执行部署。
4. **双击运行 .ps1 闪退**：脚本已内置 try/catch + `Read-Host` 暂停；若连暂停都不出现，是执行策略问题，运行 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 放行。
5. **全新环境构建报 TS7026（JSX.IntrinsicElements 不存在）**：曾因 `@types/react` / `@types/react-dom` 不在 package.json 里，换机器重装依赖后构建失败并报 200+ 类型错误。2026-07-11 已把两个类型包正式加入 devDependencies，新环境只需 `npm.cmd install` 即可。若再遇同类错误，先检查类型包是否在 package.json。
6. **命令必须在项目目录执行**：git / npm 命令在别的目录跑会报 "not a git repository" 或找不到 package.json，先 `cd "C:\project\linxiaoqing_resume_site"`。

## 8. 发布后验收清单

```text
首页打开正常，三张封面图为最新版（对照 ?v= 参数）
三个独立详情页可打开：bank.html / ecommerce.html / novel.html，meta 独立、表格完整、返回链接可用
经历区：四统计卡带底注、时间线五卡对齐、"早期业务经历"标签右下角、教育背景面板正常
联系区三个按钮可用：手机号点击后焦点进入号码、邮箱、"下载简历 PDF"（直接下载一页 A4）
手机宽度检查：无字出框、卡片不挤、详情页表格可横向滚动
页面关键内容在：林晓庆 / 业务数据分析师 / 三个项目标题 / xunni8214@gmail.com
```

## 9. 待办与后续方向

1. **银行封面真实化**：封面左图折线仍是示意。若用户能提供脱敏后的季度相对值（如指数化到 100 的趋势），可替换为真实相对序列。
2. **第四个项目卡**：中文网文研究项目（见 5.4），用户说"这次不加"，未来可提。
3. **年度数据更新**：每年 CNNIC 和统计局新报告发布后，更新电商封面、详情页表格、CSV 样例、简历和第 5.2 节，并更新 `?v=` 参数与 PDF。

## 10. 一句话记忆

```text
main 管源码，gh-pages 管线上；数字必须有来源，改图记得换 ?v=；
日常发布跑 publish-all.ps1，线上没更新先想 CDN 缓存十分钟。
```
